#Notas
Mi primer programa en Node

```
Opciones:
      --help     Muestra ayuda                                         [booleano]
      --version  Muestra número de versión                             [booleano]
  -b, --base     La base de la tabla de multiplicar                             [número] [requerido]
  -l, --listar   Ayuda a saber si se quiere listar la tabla en el log           [booleano] [defecto: false]
  -h, --hasta    Es el número hasta el cual va a ir la tabla de multiplicar     [número] [requerido]
```